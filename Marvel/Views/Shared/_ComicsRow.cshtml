@model Marvel.Models.DataContainer<Marvel.Models.Comic>

@foreach (Marvel.Models.Comic comic in Model.results)
{
    <div class="col-xs-12 col-sm-6 col-lg-3 col-thumbnail">
        @{Html.RenderPartial("_ComicThumbnail", comic);}
    </div>
}
